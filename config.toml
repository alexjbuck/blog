# The URL the site will be built for
base_url = "https://alexjbuck.github.io/blog"

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = true

# When set to "true", the generated HTML files are minified.
minify_html = false

# When set to "false", Sitemap.xml is not generated
generate_sitemap = true

[markdown]
# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = true

# The theme to use for code highlighting.
# See below for list of allowed values.
highlight_theme = "base16-ocean-dark"

# When set to "true", emoji aliases translated to their corresponding
# Unicode emoji equivalent in the rendered Markdown files. (e.g.: :smile: => 😄)
render_emoji = true

[extra]
# Put all your custom variables here

owner = "alexander \"jarvis\" buck"
site_title = "the data hangar"
site_description = "A naval aviator's journey through data engineering, where aviation meets analytics."

# Social Links
social_links = [
    { name = "GitHub", url = "https://github.com/alexjbuck" },
    { name = "LinkedIn", url = "https://linkedin.com/in/alexjbuck-101" },
    { name = "Email", url = "mailto:datahangar@alexjbuck.com" },
]
